<%= form_for(video_set) do |f| %>
  <% if video_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_set.errors.count, "error") %> prohibited this video_set from being saved:</h2>

      <ul>
      <% video_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.text_field :name, 'placeholder': 'Name', 'style':'width:100%;' %>
  </div>
  <br>
  
  <div class="field centrate">
    Image:
    <%= f.file_field :image, class: 'centrate', style: 'margin-left: 30px;display: inline;' %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit 'class': 'btn btn-success'%>
  </div>
<% end %>
