<%= form_for(remembered_word) do |f| %>
  <% if remembered_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(remembered_word.errors.count, "error") %> prohibited this remembered_word from being saved:</h2>

      <ul>
      <% remembered_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word_id %>
    <%= f.number_field :word_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
